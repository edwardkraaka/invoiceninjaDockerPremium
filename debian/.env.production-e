# Invoice Ninja Production Environment Variables
# For use with Nginx Proxy Manager on separate VPS

# Application Settings
APP_URL=https://invoicing.althams.org  # CHANGE THIS to your domain
APP_KEY=base64:vO8B2aHkTVWtGpefYzveK8nnUH2F2nMdZ35+EKpB6bM=   # Generate with: docker run --rm invoiceninja/invoiceninja-debian php artisan key:generate --show
APP_ENV=production
APP_DEBUG=false
REQUIRE_HTTPS=true
PHANTOMJS_PDF_GENERATION=false
PDF_GENERATOR=snappdf

# IMPORTANT: Trust your Nginx Proxy Manager server
# Option 1: Specific IP (most secure)
#TRUSTED_PROXIES='YOUR_NPM_SERVER_IP'
# Option 2: Trust all (easier setup, less secure)
TRUSTED_PROXIES='*'

# Cache & Session
CACHE_DRIVER=redis
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis

# Redis Configuration
REDIS_HOST=redis
REDIS_PASSWORD=CantHaCk101
REDIS_PORT=6379

# Filesystem
FILESYSTEM_DISK=debian_docker

# Database Configuration
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=ninja
DB_USERNAME=ninja
DB_PASSWORD=CantHaCk101  # CHANGE THIS!
DB_ROOT_PASSWORD=CantHaCk101  # CHANGE THIS!
DB_CONNECTION=mysql
# For production, enable SSL for database
DB_SSL_MODE=PREFERRED

# Initial Admin Account
IN_USER_EMAIL=admin@althams.org  # CHANGE THIS!
IN_PASSWORD=CantHaCk101  # CHANGE THIS!

# Mail Configuration (Gmail example)
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com  # CHANGE THIS!
MAIL_PASSWORD=your-app-specific-password  # CHANGE THIS!
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@yourdomain.com  # CHANGE THIS!
MAIL_FROM_NAME="Your Company Name"  # CHANGE THIS!

# Alternative Mail Configuration (for local mail server)
#MAIL_MAILER=smtp
#MAIL_HOST=mail.yourdomain.com
#MAIL_PORT=587
#MAIL_USERNAME=noreply@yourdomain.com
#MAIL_PASSWORD=mail_password
#MAIL_ENCRYPTION=tls

# MySQL Environment (must match DB settings above)
MYSQL_ROOT_PASSWORD=CantHaCk101  # Must match DB_ROOT_PASSWORD
MYSQL_USER=ninja
MYSQL_PASSWORD=CantHaCk101  # Must match DB_PASSWORD
MYSQL_DATABASE=ninja

# Banking Integration (optional)
NORDIGEN_SECRET_ID=
NORDIGEN_SECRET_KEY=

# Docker Environment
IS_DOCKER=true
SCOUT_DRIVER=null

# Chrome path for PDF generation
SNAPPDF_CHROMIUM_PATH=/usr/bin/google-chrome-stable

# Invoice Ninja Environment
NINJA_ENVIRONMENT=selfhost

# Security Bypass Status (for penetration testing)
# Set to true if you've applied the security bypasses
BYPASSES_APPLIED=true